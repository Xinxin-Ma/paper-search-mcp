services:
  paper-digest:
    build:
      context: .
      dockerfile: Dockerfile.digest
    container_name: daily-paper-digest
    volumes:
      - ./output:/app/output
      - ./logs:/var/log
    restart: unless-stopped